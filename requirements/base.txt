# Core runtime dependencies for DART-Planner
# -------------------------------------------------
# This file should contain ONLY the packages strictly required at runtime
# for production deployments (onboard edge computer or cloud).
# Development- and CI-specific tools belong in requirements/dev.txt or
# requirements/ci.txt respectively.

# Scientific & numerics
numpy>=1.21.0
scipy>=1.9.0

# Visualisation (optional at runtime but lightweight)
matplotlib>=3.5.0

# Comms & async infrastructure
pyzmq>=25.0.0

# Configuration & data utils
pyyaml>=6.0
pandas>=1.5.0
tqdm>=4.64.0
pydantic>=2.0.0

# Security and authentication
cryptography>=41.0.0
bcrypt>=4.0.0
# Add FastAPI for the new gateway
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
requests
# Add Socket.IO for real-time web interface
python-socketio>=5.8.0

# Simulation
airsim

# MAVLink / serial for hardware interfaces
pymavlink
pyserial

# Database and Migrations for Auth
sqlalchemy>=2.0.0
alembic
passlib[bcrypt]

# More robust JWT and security features
python-jose[cryptography]

# For CSRF form handling
python-multipart
starlette

# File watcher for key rotation
watchdog>=3.0.0 