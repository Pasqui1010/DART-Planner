# DART-Planner Environment Variables
# -----------------------------------
# Copy this file to .env and fill in the values.
# The application will load these variables on startup.

# A strong, randomly generated secret key for signing JWTs.
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
DART_SECRET_KEY="a_very_secret_key_that_should_be_changed"

# The database connection URL.
# By default, it uses a SQLite database in the user's home directory.
# You can override this to use a different database, e.g., PostgreSQL.
# Example: DART_DB_URL="postgresql://user:password@host:port/database"
DART_DB_URL="sqlite:///~/.dart_planner/auth.db" 